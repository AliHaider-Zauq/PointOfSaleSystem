@model IEnumerable<PointOfSaleSystem.Models.Supplier>

@{
    ViewData["Title"] = "Suppliers";
}

<h2>Supplier List</h2>

<a asp-action="Create" class="btn btn-primary mb-3">Add New Supplier</a>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>#</th>
            <th>Supplier Name</th>
            <th>Contact Info</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var supplier in Model)
        {
            <tr>
                <td>@supplier.Id</td>
                <td>@supplier.Name</td>
                <td>@supplier.ContactInfo</td>
                <td>
                    <div class="d-flex gap-2">
                    <a asp-action="Edit" asp-route-id="@supplier.Id" class="btn btn-sm btn-warning">Edit</a>
                    <form asp-action="Delete" asp-route-id="@supplier.Id" method="post" onsubmit="return confirm('Are you sure you want to delete this?');">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
